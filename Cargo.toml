[package]
name = "rtouch"
version = "0.2.4"
edition = "2024"

[dependencies]
dirs-next="2.0" #replaced home 0.5 in dirs-next for AIO crate that can handle more than just finding the home directory
[[bin]]
path="src/main.rs"
name="rtouch"
